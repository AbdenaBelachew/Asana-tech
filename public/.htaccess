<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirect to HTTPS (optional but recommended for SEO)
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>

# Handle 404 Error Page
ErrorDocument 404 /404.html
